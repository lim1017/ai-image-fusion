export default function ReactQueryContent() {
  return (
    <div>
      <h2 className="text-[32px] font-bold">React Query</h2>

      <h4 className="mt-3">
        Utilized <b>useInfiniteQuery</b> hook to fetch, cache, and paginate the
        list of posts. As well as <b>useMutation/useQueryClient</b> to handle
        creating new posts
      </h4>
    </div>
  );
}
